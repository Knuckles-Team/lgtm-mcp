# LGTM MCP

LGTM Observability and Alertmanager MCP Server for Agentic AI!

## Environment Variables
- `ALERTMANAGER_URL`: The base service URL.
- `LGTM_TOKEN`: The API / bearer token for authentication.

## CLI Usage
```bash
python -m lgtm_mcp --help
```
